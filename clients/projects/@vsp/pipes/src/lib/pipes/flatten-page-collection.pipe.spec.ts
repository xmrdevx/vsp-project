import { FlattenPageCollectionPipe } from './flatten-page-collection.pipe';

describe('FlattenPageCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new FlattenPageCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
