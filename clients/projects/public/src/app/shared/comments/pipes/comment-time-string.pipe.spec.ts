import { CommentTimeStringPipe } from './comment-time-string.pipe';

describe('CommentTimeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentTimeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
