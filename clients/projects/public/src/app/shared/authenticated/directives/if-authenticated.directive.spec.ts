import { VspIfAuthenticatedDirective } from './if-authenticated.directive';

describe('VspIfAuthenticatedDirective', () => {
  it('should create an instance', () => {
    const directive = null; //new VspIfAuthenticatedDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
