import { AnnouncementTimeStringPipe } from './announcement-time-string.pipe';

describe('AnnouncementTimeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnouncementTimeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
