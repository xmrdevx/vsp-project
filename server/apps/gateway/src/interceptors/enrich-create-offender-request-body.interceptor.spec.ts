import { EnrichCreateOffenderRequestBodyInterceptor } from './enrich-create-offender-request-body.interceptor';

describe('EnrichCreateOffenderRequestBodyInterceptor', () => {
  it('should be defined', () => {
    expect(new EnrichCreateOffenderRequestBodyInterceptor()).toBeDefined();
  });
});
