import { EnrichUpdateOffenderRequestBodyInterceptor } from './enrich-update-offender-request-body.interceptor';

describe('EnrichUpdateOffenderRequestBodyInterceptor', () => {
  it('should be defined', () => {
    expect(new EnrichUpdateOffenderRequestBodyInterceptor()).toBeDefined();
  });
});
