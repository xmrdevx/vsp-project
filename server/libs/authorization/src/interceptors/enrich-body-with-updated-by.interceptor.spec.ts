import { EnrichBodyWithUpdatedByInterceptor } from './enrich-body-with-updated-by.interceptor';

describe('EnrichBodyWithUpdatedByInterceptor', () => {
  it('should be defined', () => {
    expect(new EnrichBodyWithUpdatedByInterceptor()).toBeDefined();
  });
});
