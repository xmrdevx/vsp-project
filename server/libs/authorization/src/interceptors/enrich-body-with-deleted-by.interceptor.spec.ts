import { EnrichBodyWithDeletedByInterceptor } from './enrich-body-with-deleted-by.interceptor';

describe('EnrichBodyWithDeletedByInterceptor', () => {
  it('should be defined', () => {
    expect(new EnrichBodyWithDeletedByInterceptor()).toBeDefined();
  });
});
