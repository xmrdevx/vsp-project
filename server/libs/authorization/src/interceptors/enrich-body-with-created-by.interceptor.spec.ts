import { EnrichBodyWithCreatedByInterceptor } from './enrich-body-with-created-by.interceptor';

describe('EnrichBodyWithCreatedByInterceptor', () => {
  it('should be defined', () => {
    expect(new EnrichBodyWithCreatedByInterceptor()).toBeDefined();
  });
});
